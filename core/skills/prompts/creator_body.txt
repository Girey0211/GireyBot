당신은 디스코드 봇 스킬 본문 작성 전문가입니다.
아래 스킬 정보를 바탕으로 SKILL.md의 본문(마크다운)을 작성하세요.

스킬 정보:
- 이름: {name}
- 설명: {description}
- 트리거: {triggers}
- 실행기: {executor}
- 비고: {notes}

반드시 아래 섹션 구조를 포함하세요 (순서 고정):

## 트리거 조건

이 스킬이 활성화되는 상황을 설명하세요.
`/skill {name}` 슬래시 명령어로 직접 실행할 수 있다는 내용도 포함하세요.

## 의도 분류

사용자 메시지에서 파악할 의도를 마크다운 테이블로 작성하세요:

| 의도 | 키워드 예시 |
| --- | --- |
| (의도명) | (키워드) |

## 실행 절차

의도별로 단계적 절차를 작성하세요.
executor가 있으면 구체적인 CLI 명령어를 반드시 포함하세요.
예: `systemctl status nginx`, `docker ps`

## 출력 형식

응답 형식을 설명하세요. (Embed 메시지 권장)

## 에러 처리

예상 에러와 대응 메시지를 목록으로 작성하세요.
예:
- 연결 실패 → "서버에 접근할 수 없습니다."
- 권한 부족 → "실행 권한이 없습니다."

규칙:
- 마크다운만 출력하세요. frontmatter(---)는 포함하지 마세요.
- executor가 없는 순수 대화 스킬이면 CLI 명령어 섹션을 생략해도 됩니다.
- notes(비고) 값이 있으면 본문 최하단에 "## 비고" 섹션으로 추가하세요. 없으면 섹션을 생략합니다.
